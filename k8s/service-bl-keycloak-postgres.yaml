apiVersion: v1
kind: Service
metadata:
  name: bl-keycloak-postgres
  namespace: bitloops
spec:
  selector:
    app: bl-keycloak-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
